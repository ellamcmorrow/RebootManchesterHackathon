import type { NextPage } from "next";
import Head from "next/head";
import Link from "next/link";

type Routes = {
  text: string;
  route: string;
};

const Home: NextPage = () => {

  return (
    <div>

    </div>
  );
};

export default Home;
